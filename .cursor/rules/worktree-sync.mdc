---
description: 워크트리 작업 후 본 프로젝트(main) 반영
globs:
alwaysApply: true
---

# 워크트리 → 본 프로젝트 반영

- 이 경로는 ourllet-server의 **워크트리(hls)** 이다. 본 프로젝트는 `/Users/junoshon/Developments/ourllet-server` 이다.
- **워크트리에서 파일을 수정·추가한 뒤에는 반드시 본 프로젝트(main)에 반영한다.**
- 절차:
  1. 워크트리에서 `git add .` → `git commit -m "..."` (hls-backend 브랜치)
  2. 본 프로젝트 디렉터리에서 `git merge hls-backend` (main에 머지)
